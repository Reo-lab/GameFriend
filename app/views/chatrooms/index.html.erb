<%= render "layouts/sidemenu"%>

<div class = "chatroom_index_title">
  チャットルームを選択
</div>
<div class="index_chatroom_box">
  <% if @chatrooms.present? %>
    <% @chatrooms.each do |chatroom|%>
      <%= render "chatroom_info_box", chatroom: chatroom %>
    <% end %>
  <% else%>
    <%= link_to "募集への応募を受け入れる",boards_requests_path, class: 'boards_new_link' %>
  <% end %>
</div>
